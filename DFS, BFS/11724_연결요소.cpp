#include <iostream>
#include <vector>
using namespace std;

const int MAX = 1000 + 1;
int M, N;

//vector<int> graph[MAX];
int array[MAX][MAX];
bool visited[MAX];

void DFS(int current)
{
	currne	
}

int main(void)
{
	cin>> N >> M;
//	while(cin>>)? // 그 입력이 있을 때까지 계속 오는 게 뭐였지?? 

	//1. input 영역 
	for (int i = 0; i< M ; i++) //간선의 수만큼 받을 수 있도록  
	{
		int a, b;
		scanf("%d %d", &a, &b);
		
		//이런식으로 array 에 채워넣어주기 	
		array[a][b] = array[b][a];
	}
	
	for(int i = 0 ; i< N ; i++)
	{
		
	}
	
	
}

